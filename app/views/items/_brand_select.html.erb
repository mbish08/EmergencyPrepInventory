<%= f.fields_for :type do |t| %>
    <%= f.collection_select :type_id, Type.all, :id, :name, prompt: true %>
    <%= f.label :name, "Add a new storage type, if not listed" %>
    <%= t.text_field :name %>
<% end %>
<br>