<br>
<h2>Here are all of your supplies!</h2>
<br>
<dl>
    <% @user.types.uniq.each do |type| %>
        <dt><%= type.name %></dt>
        <% type.items.collect do |item| %>
            <dd>Amount of <%= link_to item.name, item_path(item) %>: <%= item.quantity %></dd>
        <% end %>
    <% end %>
</dl>