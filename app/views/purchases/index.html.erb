<h2>All the supplies but in purchases!</h2>
<ol>
    <% @purchases.each do |purchase| %>
    <li>Supply: <%= link_to purchase.item.name.capitalize, purchase_path(purchase) %> | Quantity: <%= purchase.quantity %> | <%= link_to "edit", edit_purchase_path(purchase) %></li><br>
    <% end %>
</ol>
