<h2>List of all current storage types</h2>
<ul>
    <% @types.each do |type| %>
        <li><%= type.name %> <%= link_to "View, add, or edit items in this storage type", type_items_path(type) %></li>
    <% end %>
</ul>